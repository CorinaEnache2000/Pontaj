@{
    ViewData["Title"] = "Unități organizaționale";
}

<div class="text-center">
    <h1 class="display-4">Bine ai venit, @(User.FindFirst("DisplayName")?.Value ?? User.Identity?.Name)</h1>
    <p class="text-muted">Unitățile organizaționale sunt disponibile aici.</p>
</div>